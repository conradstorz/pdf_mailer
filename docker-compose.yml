
version: "3.8"

services:
  csv_pdf_mailer:
    build: .
    container_name: csv_pdf_mailer
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/logs
      - ./.secrets:/secrets
    environment:
      - TZ=America/New_York
    restart: unless-stopped
